# 04-ota-updates — OTA Updates

expo-updates configured to auto-check on launch. Use channels: production, staging. Push JS-only fixes without store re-submission. Native code changes still require new build.

## Acceptance
- [ ] Process documented for next release cut
